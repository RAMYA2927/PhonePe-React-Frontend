services:
  - type: web
    name: phonepay-frontend
    env: static
    buildCommand: "cd frontend && npm run build"
    staticPublishPath: ./frontend/build
    routes:
      - type: rewrite
        source: /api/*
        destination: /api/*
      - type: rewrite
        source: /*
        destination: /index.html
